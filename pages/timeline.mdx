import {Steps} from "nextra/components";
import {Heading} from "@/components/timeline";
import { useData } from 'nextra/data'
import { serialize } from 'next-mdx-remote/serialize'
import { MDXRemote } from 'next-mdx-remote'

export const getStaticProps = async () => {
  const source = `### Heading 1
### Heading 2`
  const mdxSource = await serialize(source)
  return { props: { ssg: { source: mdxSource } } }
}

export const Remote = () => {
  const { source } = useData()
  return <MDXRemote {...source} />
}

# Interactive Timeline

> Ideally this would pull from user-configured selected colleges to add deadlines, etc. For example, the user selects the 6 colleges they are applying to. We pull from some database (Common App API??) to get deadlines, and then customize this to the user.
> 
> This should also allow the user to set a bunch of preferences in their account - for example, you might want to say you're not planning on applying early, and so it won't reccomend stuff. Etc. 

<Steps>
<Remote />
</Steps>

